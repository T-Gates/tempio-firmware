services:
  mosquitto:
    image: eclipse-mosquitto:2
    container_name: seonul-mosquitto
    restart: unless-stopped
    ports:
      - "1883:1883"
    volumes:
      - ./mosquitto/config/mosquitto.conf:/mosquitto/config/mosquitto.conf
      - mosquitto-data:/mosquitto/data

  server:
    build:
      context: ../server
    container_name: seonul-server
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - SEONUL_MQTT_BROKER_HOST=mosquitto
      - SEONUL_DB_PATH=/data/seonul.db
    volumes:
      - server-data:/data
    depends_on:
      - mosquitto

volumes:
  mosquitto-data:
  server-data:
